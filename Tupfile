include_rules

src_c = src/dynamic_array.c \
    src/solvers/euler.c \
    src/visualizer.c \
    src/sim.c \
    src/main.c

CFLAGS += -g
LDFLAGS += -g
LDLIBS += -lm -ldl

: foreach $(src_c) |> !cc |> out/%B.o
: out/*.o |> !ld |> out/main

.gitignore